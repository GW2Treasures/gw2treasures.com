{
  "$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["lib/**"]
		},
    "ncc": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
	}
}
