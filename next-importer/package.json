{
  "name": "next-importer",
  "scripts": {
    "prisma": "prisma generate --schema=prisma/next.prisma && prisma generate --schema=prisma/legacy.prisma",
    "build": "tsc",
    "predev": "npm run build",
    "dev": ". ./.env && node lib/index.js"
  },
  "dependencies": {
    "@prisma/client": "^4.11.0"
  },
  "devDependencies": {
    "@tsconfig/node18": "^1.0.1",
    "@types/node": "^18.11.5",
    "prisma": "^4.11.0",
    "typescript": "^4.8.4"
  }
}
