{
  "name": "@gw2treasures/database",
  "private": true,
  "scripts": {
    "prisma:generate": "retry -n 3 -- prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:migrate-dev": "prisma migrate dev --name",
    "prisma:studio": "prisma studio",
    "build": "tsc"
  },
  "main": "./lib/index.js",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.0.0",
    "prisma": "^5.0.0"
  }
}
