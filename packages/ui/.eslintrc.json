{
  "root": true,

  "extends": ["@gw2treasures/eslint-config/react", "next/core-web-vitals"],

  "rules": {
    // this is not actually a next project, so there is no pages directory
    "@next/next/no-html-link-for-pages": "off"
  },

  // eslint can't parse this file (Cannot use 'in' operator to search for 'type' in undefined)
  "ignorePatterns": ["components/Table/comparable-properties.ts"]
}
