{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "turbo": "^1.8.8"
  }
}
