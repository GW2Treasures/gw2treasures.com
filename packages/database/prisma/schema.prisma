generator client {
  provider        = "prisma-client-js"
  output          = "../.prisma"
  binaryTargets   = ["native"]
  previewFeatures = ["views"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
