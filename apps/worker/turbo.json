{
  "$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"outputs": ["dist/**"]
		},
    "ncc": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
	}
}
