{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["build.ts", "icons/*.svg"],
      "dependsOn": ["^build"]
    }
  }
}
