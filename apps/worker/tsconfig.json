{
  "extends": "@gw2treasures/tsconfig/base",
  "compilerOptions": {
    "rootDir": "./src",

    // don't emit anything, tsc is only used to validate types in worker
    "noEmit": true,
    "declaration": false,

    // TODO: remove verbatimModuleSyntax override
    "verbatimModuleSyntax": false
  },
  "include": ["./src"],
}
