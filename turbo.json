{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "prisma:generate"]
    },
    "lint": {},
    "prisma:generate": {
      "cache": false
    }
  }
}
