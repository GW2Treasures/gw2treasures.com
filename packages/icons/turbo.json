{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "build.ts",
        "icons/*.svg"
      ],
      "dependsOn": [
        "^build"
      ]
    }
  }
}
